% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xlr8.R
\name{xlr8_write_one}
\alias{xlr8_write_one}
\title{Write One Excel Report from Data and Metadata}
\usage{
xlr8_write_one(
  df,
  output_path,
  metadata_path = NULL,
  all_info = NULL,
  wb = NULL,
  sheets = NULL,
  sheets_regex = ".",
  overwrite = FALSE
)
}
\arguments{
\item{df}{A tibble of data to be written. Tables should be list-columns.}

\item{output_path}{File path where the formatted Excel workbook will be saved.}

\item{metadata_path}{(Optional) Path to the Excel template file with embedded metadata tags.}

\item{all_info}{(Optional) Pre-parsed metadata tibble from `summarize_metadata()` (specifically the `all_info` component).}

\item{wb}{(Optional) A workbook object loaded using `wb_load()`. Required if `metadata_path` is not used.}

\item{sheets}{Optional character vector of sheet names to read when parsing metadata.}

\item{sheets_regex}{Optional regular expression for matching sheets. Default is `"."`.}

\item{overwrite}{Logical. Whether to overwrite existing files. Default is `FALSE`.}
}
\value{
Invisibly returns `NULL`. The primary result is the saved Excel workbook.
}
\description{
Writes a single Excel workbook using a provided template, data frame, and metadata.
The function populates data, applies formatting styles, and saves the result.
Supports reuse of pre-parsed metadata and preloaded workbooks to enable efficient batching.
}
\examples{
\dontrun{
# Using only the template path
xlr8_write_one(df, output_path = "out.xlsx", metadata_path = "template.xlsx")

# Reusing parsed metadata and workbook
all_info <- summarize_metadata("template.xlsx") \%>\% pull_cell(all_info)
wb <- wb_load("template.xlsx")
xlr8_write_one(df, output_path = "out.xlsx", all_info = all_info, wb = wb)
}

}
